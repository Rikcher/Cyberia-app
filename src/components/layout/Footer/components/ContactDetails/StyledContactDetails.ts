import styled from "styled-components";

const StyledContactDetails = styled.div`
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    gap: 1.5625rem;
`

export default StyledContactDetails;