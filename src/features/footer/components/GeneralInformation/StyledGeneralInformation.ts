import styled from "styled-components";

const StyledGeneralInformation = styled.nav`
    display: flex;
    flex-direction: column;
    gap: 1.75rem;

    p {
        max-width: 12.5rem;
        text-wrap: wrap
    }
`

export default StyledGeneralInformation