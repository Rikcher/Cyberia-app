import styled from "styled-components";

const StyledCasesList = styled.div`
    display: grid;
    grid-template-columns: repeat(3, 1fr); 
    gap: 1.9375rem 2.5rem; 
    width: 100%; 
`

export default StyledCasesList;